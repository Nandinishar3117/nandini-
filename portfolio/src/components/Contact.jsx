import { useState } from 'react'
import { ArrowUpRight, Code, Mail, Network, Send } from 'lucide-react'
import { profile } from '../data/profile'
import SectionHeading from './SectionHeading'
export default function Contact() { const [sent, setSent] = useState(false); const submit = event => { event.preventDefault(); setSent(true); event.currentTarget.reset() }; return <section id="contact" className="section-shell contact-section section-dark"><div className="contact-grid"><div><SectionHeading eyebrow="08 / Contact" title="Let’s build something together" children="I’m always interested in opportunities to learn, collaborate, and build meaningful software." /><div className="contact-list"><a href={`mailto:${profile.email}`}><Mail size={18} />{profile.email}<ArrowUpRight size={15} /></a><a href={profile.linkedin} target="_blank" rel="noreferrer"><Network size={18} />LinkedIn profile<ArrowUpRight size={15} /></a><a href={profile.github} target="_blank" rel="noreferrer"><Code size={18} />GitHub profile<ArrowUpRight size={15} /></a></div></div><form className="contact-form reveal" onSubmit={submit}><label>Name<input name="name" type="text" placeholder="Your name" required /></label><label>Email<input name="email" type="email" placeholder="you@example.com" required /></label><label>Message<textarea name="message" rows="5" placeholder="Tell me a little about your idea..." required /></label><button className="button button-primary" type="submit">{sent ? 'Message ready to send' : 'Send message'} <Send size={16} /></button><p className="form-note">Frontend-only for now. Connect this handler to your email service or backend when ready.</p></form></div></section> }
